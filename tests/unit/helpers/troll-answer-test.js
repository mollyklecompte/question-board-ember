import { trollAnswer } from 'question-board-ember/helpers/troll-answer';
import { module, test } from 'qunit';

module('Unit | Helper | troll answer');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = trollAnswer([42]);
  assert.ok(result);
});
