import { mostUpvotes } from 'question-board-ember/helpers/most-upvotes';
import { module, test } from 'qunit';

module('Unit | Helper | most upvotes');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mostUpvotes([42]);
  assert.ok(result);
});
